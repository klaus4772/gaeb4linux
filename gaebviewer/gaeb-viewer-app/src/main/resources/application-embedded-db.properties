# Embedded PostgreSQL profile used by default.
# The app starts its own temporary PostgreSQL instance, so no local database installation is required.
# This is the recommended default for the GAEB editor.

spring.flyway.enabled=true

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>gaebviewer</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>gaeb-viewer-app</artifactId>
    <packaging>jar</packaging>

    <name>GAEB Viewer Application</name>
    <description>Main GAEB Viewer application combining core viewing and basic editing functionality</description>

    <properties>
        <java.version>17</java.version>
        <vaadin.version>24.1.3</vaadin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <version>${vaadin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Module Dependencies -->
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>gaeb-viewer-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.example</groupId>
            <artifactId>gaeb-editor-basic</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Vaadin -->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Devtools -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>gaebviewer</finalName>
        <plugins>
            <!-- Java 17 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>

            <!-- Spring Boot Fat JAR -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.example.gaebviewer.Application</mainClass>
                    <excludeDevtools>true</excludeDevtools>
                </configuration>
            </plugin>

            <!-- Bundle mit Startskripten erzeugen -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>create-bundle-scripts</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <property name="bundle.dir" value="${project.build.directory}/bundle"/>
                                <mkdir dir="${bundle.dir}"/>
                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      tofile="${bundle.dir}/${project.build.finalName}.jar"
                                      overwrite="true"/>
                                <echo file="${bundle.dir}/start.bat"><![CDATA[@echo off
setlocal
cd /d "%~dp0"
echo Starte ${project.build.finalName}.jar ...
echo Browser wird in 8 Sekunden geoeffnet auf http://localhost:8080 ...
rem Browser im Hintergrund nach 8 Sekunden oeffnen (waehrend Tomcat hochfaehrt)
start "" cmd /c "timeout /t 8 /nobreak > nul && start http://localhost:8080"
java -jar ${project.build.finalName}.jar
set EXIT_CODE=%ERRORLEVEL%
echo.
echo Anwendung beendet mit Exit-Code %EXIT_CODE%.
pause
exit /b %EXIT_CODE%
]]></echo>
                                <echo file="${bundle.dir}/start.sh"><![CDATA[#!/usr/bin/env bash
set -u

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "Starte ${project.build.finalName}.jar ..."
echo "Browser wird in 8 Sekunden geoeffnet auf http://localhost:8080 ..."

# Browser im Hintergrund nach 8 Sekunden oeffnen (waehrend Tomcat hochfaehrt)
# xdg-open fuer Linux, open fuer macOS
(sleep 8 && (xdg-open "http://localhost:8080" 2>/dev/null || open "http://localhost:8080" 2>/dev/null)) &

java -jar ${project.build.finalName}.jar
EXIT_CODE=$?

echo
read -r -p "Anwendung beendet mit Exit-Code ${EXIT_CODE}. Mit Enter schliessen ..." _
exit ${EXIT_CODE}
]]></echo>
                                <chmod file="${bundle.dir}/start.sh" perm="755"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Vaadin -->
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-maven-plugin</artifactId>
                <version>${vaadin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-frontend</goal>
                            <goal>build-frontend</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Assembly Plugin für Deployment-Paket -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <descriptors>
                                <descriptor>src/assembly/distribution.xml</descriptor>
                            </descriptors>
                            <finalName>gaebviewer-${project.version}</finalName>
                            <appendAssemblyId>true</appendAssemblyId>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>prod</id>
            <properties>
                <vaadin.productionMode>true</vaadin.productionMode>
            </properties>
        </profile>
    </profiles>

</project>






